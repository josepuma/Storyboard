export class HitCircle {
    constructor(x, y, startTime, sample, hitSound){
        this.x = x
        this.y = y
        this.startTime = startTime
        this.sample = sample
        this.hitSound = hitSound
    }
}